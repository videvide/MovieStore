@{
    ViewBag.Title = "Admin Page";
}

<h2>Admin Page</h2>

@Html.ActionLink("Roles", "Roles", new { }, new { @class = "btn btn-primary" })
@Html.ActionLink("Customers", "Customers", new { }, new { @class = "btn btn-primary" })